# Carte d'anniversaire pour Taha

Une carte d'anniversaire numérique et interactive dédiée à Taha. L'application propose un design festif, des animations douces et une pluie de confettis déclenchée à la demande pour célébrer la journée.

## 🚀 Démarrage

```bash
npm run dev
```

Ouvrez [http://localhost:3000](http://localhost:3000) pour voir la carte.

## 🧩 Technologies

- Next.js 14 (App Router, TypeScript)
- next/font pour le chargement optimisé des polices Google
- CSS Modules pour le style encapsulé

## 📦 Scripts

- `npm run dev` – lance le serveur de développement
- `npm run build` – construit le bundle de production
- `npm start` – démarre le serveur en production
- `npm run lint` – exécute ESLint

## 🖼️ Personnalisation

- Modifiez `src/app/page.tsx` pour changer l'en-tête ou ajouter des sections
- Ajustez `src/components/BirthdayCard.tsx` et `BirthdayCard.module.css` pour affiner le message et l'esthétique
- Les couleurs et effets globaux se trouvent dans `src/app/globals.css`
